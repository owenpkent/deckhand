# Rust / Tauri
target/
**/src-tauri/target/
**/src-tauri/gen/
*.pdb

# Node / frontend
node_modules/
dist/
dist-ssr/
.vite/
*.tsbuildinfo
package-lock.json

# Bundled installers and signing material
release/
*.msi
*.nsis.zip
*.sig
*.p12
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini
*.stackdump

# Spike evidence logs (findings are recorded in docs/, raw logs stay local)
spike-log.jsonl

# AI/LLM scratch space
_scratch/
_draft_*
*.tmp
.cascade/
.cursor/

# Claude Code local overrides (.claude/settings.json itself is committed:
# it holds the shared style gate). The *.local pattern below does not
# match this filename.
.claude/settings.local.json

# Local runtime state (never commit real session data)
*.local
.env
.env.*
!.env.example
deckhand.log
sessions.json

# Python (docs tooling, spec linters)
__pycache__/
*.py[cod]
venv/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
