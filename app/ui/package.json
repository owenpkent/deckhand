{
  "name": "deckhand-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Deckhand surface: TypeScript compiled by tsc, no bundler",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
